"""Just a simple example module to have some initial structure."""


def add(a: float, b: float) -> float:
    """Add two numbers.

    Args:
        a: A number.
        b: Another number.

    Returns:
        The sum of a and b.
    """
    return a + b
